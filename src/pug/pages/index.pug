extends ../common/layout.pug

block vars
  -var mainPage = true
  -var currentPageTitle = site.index.title
  -var currentPageHref = site.index.href

block mainContent
  .overlay
  include ../blocks/recently.pug
  include ../blocks/news.pug
  include ../blocks/reviews.pug
